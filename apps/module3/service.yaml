apiVersion: v1
kind: Service
metadata:
  name: module3
  namespace: default
  labels:
    app: module3
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: module3